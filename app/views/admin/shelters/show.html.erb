<h1><%= @sql_shelter_name %></h1>
<h3><%= @sql_shelter_city %></h3>
<div class="statistics">
  <h3>Statistics:</h3>
  <p><%= "Average age of adoptable pets in this shelter is #{@shelter.avg_age}"%></p>
  <p><%= "There are #{@shelter.adoptable_pet_count} adoptable pets in this shelter" %></p>
  <p><%= "#{@shelter.adopted_pet_count} pets have been adopted from this shelter" %></p>
</div>

<div class="action_required">
  <h3>Action Required</h3>
  <% @pending_pet_apps.each do |pet_app| %>
    <div class=<%= "pet_app_#{pet_app.id}" %>>
      <p><%= pet_app.pet.name %></p>
      <p><%= "Approve or Reject #{pet_app.application.name}'s application " %><%= link_to "here" , "/admin/applications/#{pet_app.application.id}"%></p>
    </div>
  <% end %>
</div>
